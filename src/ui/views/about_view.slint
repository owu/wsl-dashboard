import { Theme, AppInfo, AppI18n, AboutStrings } from "../theme.slint";

export component AboutView inherits VerticalLayout {
    in property <AboutStrings> strings;
    spacing: 20px;
    padding-left: 4px;
    padding-right: 16px;
    padding-top: 8px;
    padding-bottom: 8px;
    alignment: start;
    callback open_url(string);
    callback check_update_clicked();
    Rectangle {
        background: Theme.card_bg;
        border-radius: 8px;
        border-width: 1px;
        border-color: Theme.border_color;
        VerticalLayout {
            spacing: 15px;
            padding: 8px;
            VerticalLayout {
                spacing: 24px;
                Text {
                    text: root.strings.title;
                    font-size: 14px;
                    font-family: Theme.default_font;
                    font-weight: 500;
                    color: Theme.text_primary;
                }

                Text {
                    text: root.strings.description;
                    font-size: 14px;
                    font-family: Theme.default_font;
                    color: Theme.text_secondary;
                    wrap: word-wrap;
                    min-height: 80px;
                }
            }

            Rectangle {
                height: 1px;
                background: Theme.border_color;
            }

            VerticalLayout {
                spacing: 20px;
                VerticalLayout {
                    spacing: 6px;
                    Text {
                        text: root.strings.version;
                        font-size: 13px;
                        font-family: Theme.default_font;
                        font-weight: 500;
                        color: Theme.text_primary;
                    }

                    HorizontalLayout {
                        spacing: 6px;
                        alignment: start;
                        Text {
                            text: "v" + AppInfo.version;
                            font-size: 13px;
                            font-family: Theme.default_font;
                            color: Theme.text_secondary;
                            TouchArea {
                                mouse-cursor: pointer;
                                clicked => {
                                    root.check_update_clicked();
                                }
                            }
                        }

                        if (AppInfo.checking_update): Text {
                            text: "(" + root.strings.check_update + ")";
                            font-size: 12px;
                            font-family: Theme.default_font;
                            color: Theme.accent;
                        }
                        if (!AppInfo.checking_update && AppInfo.has_update): Text {
                            text: "(" + AppI18n.t("about.new_version", [AppInfo.latest_version, AppI18n.version]) + ")";
                            font-size: 12px;
                            font-family: Theme.default_font;
                            color: #FF4D4F;
                            TouchArea {
                                mouse-cursor: pointer;
                                clicked => {
                                    root.open_url(AppInfo.release_url);
                                }
                            }
                        }
                    }
                }

                VerticalLayout {
                    spacing: 6px;
                    Text {
                        text: root.strings.website;
                        font-size: 13px;
                        font-family: Theme.default_font;
                        font-weight: 500;
                        color: Theme.text_primary;
                    }

                    Text {
                        text: AppInfo.homepage;
                        font-size: 13px;
                        font-family: Theme.default_font;
                        color: Theme.accent;
                        TouchArea {
                            mouse-cursor: pointer;
                            clicked => {
                                root.open_url(AppInfo.homepage);
                            }
                        }
                    }
                }

                VerticalLayout {
                    spacing: 6px;
                    Text {
                        text: root.strings.issues;
                        font-size: 13px;
                        font-family: Theme.default_font;
                        color: Theme.text_primary;
                        TouchArea {
                            mouse-cursor: pointer;
                            clicked => {
                                root.open_url(AppInfo.issues_url);
                            }
                        }
                    }
                }

                VerticalLayout {
                    spacing: 6px;
                    Text {
                        text: root.strings.discussions;
                        font-size: 13px;
                        font-family: Theme.default_font;
                        color: Theme.text_primary;
                        TouchArea {
                            mouse-cursor: pointer;
                            clicked => {
                                root.open_url(AppInfo.discussions_url);
                            }
                        }
                    }
                }
            }
        }
    }
}
